package com.javamaster.springwithjpa;

import org.springframework.data.jpa.repository.JpaRepository;

public interface robotDescriptionSQLRepository extends JpaRepository<robotDescriptionSQL, Long> {
}